import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'subscriber-inject.example.domain.com',
    title: 'Subscriber Inject',
  },
};
export default env;
