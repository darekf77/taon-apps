import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'request-failure.example.domain.com',
    title: 'Request Failure',
  },
};
export default env;
