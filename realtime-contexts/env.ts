import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'realtime-contexts.example.domain.com',
    title: 'Realtime Contexts',
  },
};
export default env;
