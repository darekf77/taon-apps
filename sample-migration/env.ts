import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'sample-migration.example.domain.com',
    title: 'Sample Migration',
  },
};
export default env;
